package com.algo.one_array;

public class RoatedSortedArraySearch {


/*   we check if (nums[left] <= nums[mid]) then left side is sorted
    but still right side is sorted
    Yes — the right side may also be sorted, but the condition still works correctly.
    The algorithm does not require the other side to be unsorted.
    It only needs at least one side to be sorted.
    Neither we need the mid to come at pivot of sorted and unsorted

    */



}
